/**
 * Message Template Layout Manager
 * @module layouts/messages
 */

export const start: string =
  `<b>Konichiwa!</b>` +
  `\n` +
  `\n` +
  `I'm assistant of WIUT Anime Club and I do love gambling very much!` +
  `\n` +
  `\n` +
  `I was created in order to help and with my help, you can do:` +
  `\n` +
  `\n` +
  `<code>* Get information about anime related things</code>` +
  `\n` +
  `<code>* Register to upcoming anime events</code>` +
  `\n` +
  `\n` +
  `Let the gambling begin and let's gamble like crazy!!!` +
  `\n` +
  `\n` +
  `<i>In order to see full detailed usage information of the bot, press the button below.</i>`;

export const help =
  `<b>List of available commands:</b>` +
  `\n` +
  `\n` +
  `/help - <code>show this helper message</code>` +
  `\n` +
  `/ticket - <code>get a ticket for upcoming event</code>` +
  `\n` +
  `/clubs - <code>show url links of club's chat</code>`;

export const invalid = `<b>Hey, I didn't get this command or message. Please see my command list for more information!</b>`;

export const clubs = `<b>Here are the current clubs supporting anime audience at WIUT:</b>`;
